import { extendTheme } from "@chakra-ui/react";

const chakraTheme = extendTheme({
  styles: {
    global: {
      body: {
        background: "#DDEBE6",
      },
    },
  },
});

export default chakraTheme;
